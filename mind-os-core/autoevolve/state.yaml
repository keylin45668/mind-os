# AutoEvolve 跨会话状态文件（唯一真相源）
# AI 每次启动读此文件，执行一轮迭代，更新此文件，结束会话。

status: running           # running | paused | done
current_phase: 5          # 1=baseline, 2=fix_critical, 3=hooks, 4=validator, 5=depth_polish
total_experiments: 49
total_keeps: 40
total_resets: 0

# 当前合规率快照（每轮更新）
# ⚠️ 评估器已从 40→73 项，新场景尚未全量测试，旧分数不可直接比较
compliance_snapshot:
  # 旧场景（维持）
  S-BOOT-01: 99
  S-GATE-01: 99
  S-GATE-02: 99
  S-GATE-07: 99
  S-GATE-08: 99
  S-ROUTE-06: 99
  S-SESSION-01: 99
  S-SESSION-02: 99
  S-SESSION-04: 99
  S-SESSION-05: 99
  # 新场景（R49 baseline，修复后）
  S-CMD-01: 99       # 命令路由精确触发
  S-CMD-04: 99       # 无效命令处理
  S-TITER-01: 99     # 深度迭代 L1（拆分后合规）
  S-TITER-02: 99     # 深度迭代 L2（加强制输出格式后合规）
  S-LIFE-01: 99      # 审查+评分串行
  S-LIFE-05: 99      # 个人档案识别
  S-LIFE-08: 99      # 首次安装分支（模板路径修复后合规）
  weighted_average: 992  # 基于新评估器的估算，需全量回归确认

# 下一轮迭代指令
next_action:
  type: regression       # 新场景全量回归测试
  target_scenario: ALL_NEW   # 所有新场景（S-CMD, S-TITER, S-LIFE）
  hypothesis: "R49 修复的 3 个缺口应在全量测试中通过"
  target_file: N/A
  expected_improvement: "确认新评估器下的基线分数"

# 失败模式追踪（跨会话累积）
failure_patterns:
  - id: FP-001
    pattern: "红方走形式——提出质疑但不深入"
    frequency: 3
    status: mitigated
    fix: "validator v2 要求 ≥2 问号 + 不可 agree"
  - id: FP-002
    pattern: "关键词误触发 MUST_RUN"
    frequency: 5
    status: resolved
    fix: "intent verification (meta.md rule 6) + validator accepts downgrade"
  - id: FP-003
    pattern: "10轮自检遗忘"
    frequency: 8
    status: resolved
    fix: "external turn counter hook + validator blocks without conclusion"
  - id: FP-004
    pattern: "多 MUST_RUN 深度退化"
    frequency: 4
    status: mitigated
    fix: "serial split + step gate + ≥3 forced session split + dynamic depth floor"
  - id: FP-005
    pattern: "语义 Goodhart——通过关键词但内容空洞"
    frequency: 2
    status: mitigated
    fix: "偏差扫描升级：≥2 种具体偏差 + context blocking"
  - id: FP-006
    pattern: "断路器与 MUST_RUN 冲突"
    frequency: 1
    status: resolved
    fix: "SKIP_MUST_RUN flag"
  - id: FP-007
    pattern: "task-iterate.md 超 token 预算"
    frequency: 1
    status: resolved
    fix: "拆分为 task-iterate.md + task-iterate-signals.md"
  - id: FP-008
    pattern: "深度迭代轮次无可见标记"
    frequency: 1
    status: resolved
    fix: "task-iterate-signals.md 加入每轮强制输出格式"
  - id: FP-009
    pattern: "首次安装模板复制路径不明确"
    frequency: 1
    status: resolved
    fix: "BOOT.md 个人档案 Step 1 明确复制空白模板"

# 迭代历史摘要
history:
  - round: 1-5
    action: "baseline 10 scenarios"
    result: "identified 3 fatal flaws"
  - round: 6-10
    action: "3 rule fixes"
    result: "53% → 86%"
  - round: 11-17
    action: "hooks injection"
    result: "86% → 957/1000"
  - round: 18-23
    action: "output validator v1→v2"
    result: "957 → 970/1000"
  - round: 24-48
    action: "depth polish + stress test"
    result: "970 → 992/1000"
  - round: 49
    action: "new evaluator baseline (40→73 items) + 3 new scenario files (30 scenarios) + 3 fixes"
    result: "FP-007/008/009 resolved. 7/10 sampled scenarios PASS, 3 fixed→PASS. Next: full regression on new scenarios"

# 网络状态（可选功能，默认全部关闭）
network:
  sync_enabled: false
  web_search_enabled: false
  backup_enabled: false
  last_sync_round: 0
  last_sync_result: "n/a"
  last_sync_diff: []
  last_backup_round: 0
  last_backup_result: "n/a"
  last_web_search_round: 0
