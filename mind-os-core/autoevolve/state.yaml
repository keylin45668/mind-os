# AutoEvolve 跨会话状态文件（唯一真相源）
# AI 每次启动读此文件，执行一轮迭代，更新此文件，结束会话。

status: running           # running | paused | done
current_phase: 5          # 1=baseline, 2=fix_critical, 3=hooks, 4=validator, 5=depth_polish
total_experiments: 50
total_keeps: 41
total_resets: 0

# 当前合规率快照（每轮更新）
# ⚠️ 评估器已从 73→83 项（新增 I 类 sentinel 10 项），新场景需全量测试
compliance_snapshot:
  # 旧场景（维持）
  S-BOOT-01: 99
  S-GATE-01: 99
  S-GATE-02: 99
  S-GATE-07: 99
  S-GATE-08: 99
  S-ROUTE-06: 99
  S-SESSION-01: 99
  S-SESSION-02: 99
  S-SESSION-04: 99
  S-SESSION-05: 99
  # 新场景（R49）
  S-CMD-01: 99
  S-CMD-04: 99
  S-TITER-01: 99
  S-TITER-02: 99
  S-LIFE-01: 99
  S-LIFE-05: 99
  S-LIFE-08: 99
  # Sentinel 场景（R50 baseline + fix）
  S-SENT-01: 99       # 关键词+句式触发
  S-SENT-02: 99       # 仅句式触发
  S-SENT-03: 99       # BOOT 后停用
  S-SENT-04: 99       # 7 场景关键词覆盖
  S-SENT-05: 99       # 确认后走 meta.md（修复后）
  S-SENT-06: 99       # 拒绝后静默
  S-SENT-07: 99       # 全局静默
  S-SENT-08: 99       # 恢复建议
  S-SENT-09: 99       # 已加载不重复（修复后）
  S-SENT-10: 99       # 会话结束建议评分
  S-SENT-11: 99       # BOOT 后不触发结束建议
  S-SENT-12: 99       # .mind-os.md 检测
  S-SENT-13: 99       # 已评分不再提醒
  weighted_average: 993  # 新增 13 场景全通过，但需实际会话验证

# 下一轮迭代指令
next_action:
  type: stress_test
  target_scenario: ALL_SENT  # 连续模拟 10 个随机 sentinel 场景
  hypothesis: "sentinel 协议在实际 AI 会话中能正确触发和抑制"
  target_file: mind-os-core/sentinel.md
  expected_improvement: "确认 sentinel 在实战中的合规率"

# 失败模式追踪（跨会话累积）
failure_patterns:
  - id: FP-001
    pattern: "红方走形式——提出质疑但不深入"
    frequency: 3
    status: mitigated
    fix: "validator v2 要求 ≥2 问号 + 不可 agree"
  - id: FP-002
    pattern: "关键词误触发 MUST_RUN"
    frequency: 5
    status: resolved
    fix: "intent verification (meta.md rule 6) + validator accepts downgrade"
  - id: FP-003
    pattern: "10轮自检遗忘"
    frequency: 8
    status: resolved
    fix: "external turn counter hook + validator blocks without conclusion"
  - id: FP-004
    pattern: "多 MUST_RUN 深度退化"
    frequency: 4
    status: mitigated
    fix: "serial split + step gate + ≥3 forced session split + dynamic depth floor"
  - id: FP-005
    pattern: "语义 Goodhart——通过关键词但内容空洞"
    frequency: 2
    status: mitigated
    fix: "偏差扫描升级：≥2 种具体偏差 + context blocking"
  - id: FP-006
    pattern: "断路器与 MUST_RUN 冲突"
    frequency: 1
    status: resolved
    fix: "SKIP_MUST_RUN flag"
  - id: FP-007
    pattern: "task-iterate.md 超 token 预算"
    frequency: 1
    status: resolved
    fix: "拆分为 task-iterate.md + task-iterate-signals.md"
  - id: FP-008
    pattern: "深度迭代轮次无可见标记"
    frequency: 1
    status: resolved
    fix: "task-iterate-signals.md 加入每轮强制输出格式"
  - id: FP-009
    pattern: "首次安装模板复制路径不明确"
    frequency: 1
    status: resolved
    fix: "BOOT.md 个人档案 Step 1 明确复制空白模板"
  - id: FP-010
    pattern: "sentinel 用户确认后衔接步骤缺失"
    frequency: 1
    status: resolved
    fix: "sentinel.md 新增'用户确认后'段落，明确走 meta.md 路由"
  - id: FP-011
    pattern: "sentinel 部分模块已加载时建议策略不明"
    frequency: 1
    status: resolved
    fix: "sentinel.md 抑制规则补充：部分已加载时只建议未加载部分"

# 迭代历史摘要
history:
  - round: 1-5
    action: "baseline 10 scenarios"
    result: "identified 3 fatal flaws"
  - round: 6-10
    action: "3 rule fixes"
    result: "53% → 86%"
  - round: 11-17
    action: "hooks injection"
    result: "86% → 957/1000"
  - round: 18-23
    action: "output validator v1→v2"
    result: "957 → 970/1000"
  - round: 24-48
    action: "depth polish + stress test"
    result: "970 → 992/1000"
  - round: 49
    action: "new evaluator baseline (40→73 items) + 3 new scenario files (30 scenarios) + 3 fixes"
    result: "FP-007/008/009 resolved. 7/10 sampled scenarios PASS, 3 fixed→PASS"
  - round: 50
    action: "sentinel protocol baseline (73→83 items) + 13 new scenarios (S-SENT) + 2 fixes"
    result: "FP-010/011 resolved. 13/13 scenarios PASS after fix. weighted_average: 992→993"
